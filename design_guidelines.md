# 麻将约局 H5 应用设计指南

## 1. 品牌定位

- **应用定位**：轻量化麻将约局工具
- **设计风格**：国风简约麻将主题
- **目标用户**：麻将爱好者，方便快速发起和响应约局
- **核心体验**：简洁高效，一键创建/加入房间

## 2. 配色方案

### 主色调
- **主色（Primary）**：`#B91C1C`（中国红）- 用于主要按钮、重要提示
- **金色（Gold）**：`#D4AF37` - 装饰元素、高亮文字
- **深灰（Dark Gray）**：`#374151` - 正文文字、次要元素

### 辅助色
- **背景色**：`#FAFAF9`（米白）- 页面背景
- **卡片色**：`#FFFFFF`（纯白）- 卡片容器
- **边框色**：`#E5E5E5`（浅灰）- 分割线、边框
- **成功色**：`#059669`（翠绿）- 加入成功状态
- **警示色**：`#DC2626`（深红）- 拒绝、满员提示

### Tailwind 色值对应
```css
--primary: #B91C1C          /* text-red-700, bg-red-700 */
--primary-foreground: #FFFFFF
--secondary: #374151         /* 深灰 */
--accent: #D4AF37           /* 金色 */
--background: #FAFAF9
--card: #FFFFFF
--foreground: #374151
--muted: #F5F5F4
--muted-foreground: #6B7280
--border: #E5E5E5
--destructive: #DC2626
--success: #059669
```

## 3. 字体规范

- **标题**：深灰色（#374151），字号 18-24px
- **正文**：深灰色（#374151），字号 14-16px
- **辅助文字**：灰色（#6B7280），字号 12-14px
- **强调文字**：金色（#D4AF37），用于装饰性文字

## 4. 间距系统

- **页面边距**：`px-4`（16px）
- **卡片内边距**：`p-4` 至 `p-6`（16-24px）
- **元素间距**：`gap-3` 至 `gap-4`（12-16px）
- **卡片间距**：`mb-4`（16px）

## 5. 组件选型原则

### 必须使用组件库
- **按钮**：使用 `@/components/ui/button`
- **输入框**：使用 `@/components/ui/input`
- **卡片**：使用 `@/components/ui/card`
- **标签/徽章**：使用 `@/components/ui/badge`
- **对话框**：使用 `@/components/ui/dialog`

### 禁止手搓通用组件
- 禁止用 `View/Text` 手搓按钮、输入框、卡片、标签
- 如组件库缺失，先补齐再使用

## 6. 页面结构

### 首页（pages/index/index）
- 中央展示应用 logo/标题
- 主按钮：【发起麻将约局】
- 国风装饰元素

### 发起页面（pages/create/index）
- 表单区域（卡片容器）
  - 地点输入（必填）
  - 开始时间选择（必填）
  - 结束时间选择（选填）
- 确认创建按钮

### 房间详情页（pages/room/[id]/index）
- 房间信息卡片
  - 地点、开始/结束时间
- 成员列表（已加入人数/上限）
- 邀约链接复制按钮
- 加入/拒绝按钮

### 已满提示页（pages/room/full）
- 满员提示图标
- 提示文案
- 返回首页按钮

## 7. 交互规则

### 创建房间
- 必填项：地点、开始时间
- 生成唯一房间ID（6位随机码）
- 创建后跳转房间详情页

### 加入房间
- 点击邀约链接进入详情页
- 满员时显示满员提示页
- 加入成功显示成功提示

### 数据存储
- 使用 localStorage 持久化
- 房间数据、成员数据分开存储
- 每人只能加入同一房间一次（按 localStorage 记录）

## 8. 移动端适配

- 全屏适配，`100vh` 高度
- 触摸友好的按钮尺寸（高度 44px+）
- 圆角卡片设计（`rounded-2xl`）
- 阴影适度（`shadow-md`）

## 9. 国风元素

- 麻将图案装饰
- 红金配色呼应
- 圆角+精致阴影
- 简洁线条装饰
